local registeryServer = {
    register = "register",
    list = "list"
}

local gateServer = {
    ping = "ping",
    dial = "dial",
    disconnect = "disconnect",
    status = "status"
}

local client = {

}

return {
    registeryServer = registeryServer,
    gateServer = gateServer,
    client = client
}
